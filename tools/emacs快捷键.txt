C-x C-c   关闭 emacs
C-x C-f   打开或创建一个文件
C-x C-s   保存文件
C-x o     在 buffer 间切换
C-y       复制
C-k       Cut (kill) from the cursor to the end of the line
C-x 2     Split the window into 2 buffers
C-x 0     Undo window-splitting so there is only 1 buffer
C-x C-b   See a list of all current buffers

C-c C-s   split the window and create an SML prompt in a new buffer
C-h       帮助
C-g       中止当前命令
C-d       end a REPL session
C-c C-c   interrupt REPL running
M-p       REPL 输入上翻
M-n       REPL 输入下翻